// UTILITY FUNCTION:
// ----------------------------------
// Returns 1 x random num, between 0 and a given maximum

function generateRandomNum(max) {
  return Math.floor(Math.random() * max);
}

export default generateRandomNum;
// ----------------------------------
